import { NormalizeMessagePipe } from './normalize-message.pipe';

describe('NormalizeMessagePipe', () => {
  it('should be defined', () => {
    expect(new NormalizeMessagePipe()).toBeDefined();
  });
});
